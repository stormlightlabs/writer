[package]
name = "writer-md"
version = "0.1.0"
edition = "2024"

[dependencies]
comrak = "0.50"
docx-rs = "0.4"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2"
toml = "0.9"

writer-core = { path = "../core" }

[dev-dependencies]
serde_json = "1"
