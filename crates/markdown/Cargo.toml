[package]
name = "writer-md"
version = "0.1.0"
edition = "2024"

[dependencies]
comrak = "0.50"
serde = { version = "1", features = ["derive"] }
thiserror = "2"

writer-core = { path = "../core" }

[dev-dependencies]
serde_json = "1"
